# oiiaw 0.1.7 — 연속 저장 충돌 오탐 근본 수정

Obsidian의 빠른 연속 저장과 iCloud의 지연 확인이 겹칠 때, 직전 로컬 저장본을 독립적인 원격 편집으로 오인하던 구조를 수정합니다.

## 주요 변경 사항

- iCloud로 보낸 최근 로컬 콘텐츠 해시를 파일별로 기록합니다.
- 판정 로직을 `SyncObservation → SyncDecision` 순수 상태 머신으로 분리합니다.
- 로컬 저장 세대와 삭제 tombstone을 SQLite `sync_state.db`에 트랜잭션으로 기록합니다.
- 기존 `sync_state.json` 상태는 최초 실행 시 자동 이관하며 원본도 보존합니다.
- iCloud가 이전 로컬 세대를 재노출하면 충돌본을 만들지 않고 최신 로컬 문서를 다시 전송합니다.
- 기준본 확정 뒤 늦게 재등장한 이전 로컬 세대도 로컬 문서를 되돌리지 못하게 차단합니다.
- 출처를 확인할 수 없는 실제 원격 편집만 충돌로 분류합니다.
- 실제 충돌 사본은 `%APPDATA%\\oiiaw\\recovery` 아래의 Vault별 저장소에 격리합니다.
- 세션 누적 충돌 수와 현재 검토가 필요한 복구 사본 수를 분리해 표시합니다.
- 복구 저장소가 로컬 또는 iCloud Vault 내부에 놓이는 잘못된 설정을 시작 전에 차단합니다.
